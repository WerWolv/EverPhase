package com.deltabase.everphase.api.event.entity;

import com.deltabase.everphase.api.event.Event;
import com.deltabase.everphase.damageSource.DamageSource;
import com.deltabase.everphase.entity.Entity;

public class EntityDeathEvent extends Event {

    public EntityDeathEvent(Entity entity, DamageSource damageSource) {

    }
}
